// the angular module is defined here.  And that's all it does.
// In this case, we know we will be doing angular routing, so we include the ngRoute module with the app
angular.module('CoffeeApp', ['ngRoute'])
    